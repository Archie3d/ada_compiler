-- The root of the predefined language environment.  Everything the compiler
-- supplies is a child of this package, which is why a program has to name it
-- as Ada.Text_IO rather than Text_IO.

package Ada is

end Ada;
